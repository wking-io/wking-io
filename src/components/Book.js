import React from 'react';

import BookTitle from './BookTitle';

const Book = (props) => {
  const book = props.book;
  const title = props.title;
  console.log(title);

  return (
    <section></section>
  );
};

export default Book;
